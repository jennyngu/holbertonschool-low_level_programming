#include <stdio.h>
/**
*main - print all numbers of base 16 in lowercase
*
*Return: will always return 0
*/
int main(void)
{
int i;
int j;
for (i = 0; i < 9; i++)
{
putchar (i);
if (i == 9)
{
for (j = 'a'; j <= 'f'; j++)
{
putchar ('j');
if (j == 'f')
}
}
}
putchar('\n');
return (0);
}
