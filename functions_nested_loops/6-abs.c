#include "main.h"
/**
 *_abs - returns the absolute value of a number
 *@i: is the input number to be tested
 *Return: will be 0 if successful
 */

int _abs(int i)
{
if (i < 0)
{
return (i * (-1));
}
else
{
return (i);
}
}
